---
phase: 03-static-feature-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - front-end/src/react/components/files/FileList.tsx
  - front-end/src/react/components/image/ImageSelector.tsx
  - front-end/src/react/hooks/useFiles.ts
autonomous: true
requirements:
  - FILE-01
  - FILE-02
user_setup: []

must_haves:
  truths:
    - "User can view a list of previously uploaded images"
    - "User sees image thumbnails in the list"
    - "User can click an image to select it"
    - "Selected image is highlighted in the list"
    - "User can open a modal to browse and select images"
    - "Modal displays images in a grid layout"
    - "Image selection triggers callback with selected image"
  artifacts:
    - path: "front-end/src/react/components/files/FileList.tsx"
      provides: "File list component for displaying available images"
      min_lines: 80
    - path: "front-end/src/react/components/image/ImageSelector.tsx"
      provides: "Modal component for browsing and selecting images"
      min_lines: 120
    - path: "front-end/src/react/hooks/useFiles.ts"
      provides: "Hook for fetching and managing file list"
      min_lines: 60
  key_links:
    - from: "front-end/src/react/components/files/FileList.tsx"
      to: "front-end/src/react/hooks/useFiles.ts"
      via: "useFiles hook"
      pattern: "useFiles"
    - from: "front-end/src/react/hooks/useFiles.ts"
      to: "FileService"
      via: "fileService.listAvailableImages() call"
      pattern: "listAvailableImages"
    - from: "front-end/src/react/components/image/ImageSelector.tsx"
      to: "front-end/src/react/hooks/useFiles.ts"
      via: "useFiles hook"
      pattern: "useFiles"
    - from: "front-end/src/react/components/image/ImageSelector.tsx"
      to: "front-end/src/react/components/files/FileList.tsx"
      via: "FileList component for grid display"
      pattern: "FileList"
---

<objective>
Build file management components that allow users to browse, view, and select previously uploaded images for processing.

Purpose: Enable re-use of uploaded images without re-uploading
Output: FileList component, ImageSelector modal, useFiles hook
</objective>

<execution_context>
@/home/jrb/code/cuda-learning/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jrb/code/cuda-learning/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-static-feature-ui/03-RESEARCH.md

@front-end/src/infrastructure/data/file-service.ts
@front-end/src/components/image/image-selector-modal.ts
@front-end/src/gen/file_service_connect.ts
@front-end/src/gen/config_service_pb.ts

# Key types

From front-end/src/gen/config_service_pb.ts:
```typescript
class StaticImage {
  id: string;
  displayName: string;
  path: string;  // URL to image
  isDefault: boolean;
}
```

# gRPC API

From front-end/src/gen/file_service_connect.ts:
```typescript
export const FileService = {
  listAvailableImages: {
    name: "ListAvailableImages",
    I: ListAvailableImagesRequest,
    O: ListAvailableImagesResponse,
  },
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useFiles hook</name>
  <files>front-end/src/react/hooks/useFiles.ts</files>
  <action>
Create a custom hook `useFiles` that handles fetching the list of available images:

1. State management:
   - `images: StaticImage[]` - list of available images
   - `loading: boolean` - loading state
   - `error: string | null` - error message

2. `fetchImages()` function:
   - Call `fileService.listAvailableImages()` from existing service
   - On success: set `images` from response
   - On error: set error message, show toast error

3. Return: `{ images, loading, error, fetchImages }`

Follow the pattern from `front-end/src/react/hooks/useFilters.ts` for error handling and state management.
Import `fileService` from `@/infrastructure/data/file-service`.
Import type `StaticImage` from `@/gen/config_service_pb`.
Use `useServiceContext` from `@/react/context/service-context` (if needed for clients).
Use `useToast` from `@/react/hooks/useToast` for error notifications.
  </action>
  <verify>
    <automated>cd front-end && npx vitest run src/react/hooks/useFiles.test.tsx</automated>
  </verify>
  <done>
Hook provides `images`, `loading`, `error`, `fetchImages`.
Fetches images from fileService.listAvailableImages().
Shows toast error on failure.
</done>
</task>

<task type="auto">
  <name>Task 2: Create FileList component</name>
  <files>front-end/src/react/components/files/FileList.tsx, front-end/src/react/components/files/FileList.module.css</files>
  <action>
Create a React component `FileList` that displays available images in a list or grid:

1. Component props:
   - `images: StaticImage[]` - list of images to display
   - `selectedImageId?: string` - ID of currently selected image
   - `onImageSelect: (image: StaticImage) => void` - callback when image is selected
   - `layout?: 'list' | 'grid'` - display layout (default: 'grid')
   - `loading?: boolean` - loading state (optional, default: false)

2. UI structure:
   - Loading state: spinner or "Loading..." text
   - Empty state: "No images available" message
   - Grid/List container
   - Each image item:
     - Image thumbnail (120x120, object-fit: cover)
     - Image name (truncate if too long)
     - "Default" badge if `isDefault: true`
     - Selected state styling (border highlight)
     - Hover effect (scale transform, shadow)

3. Grid layout (default):
   - CSS Grid: `grid-template-columns: repeat(auto-fill, minmax(160px, 1fr))`
   - Gap: 16px

4. List layout:
   - Flex column
   - Each item: flex row (thumbnail left, info right)

5. Selection:
   - Click on item: call `onImageSelect(image)`
   - Selected item: distinctive border color, background change

6. Styling:
   - Use CSS Modules (`FileList.module.css`)
   - Reuse CSS custom properties from Lit
   - Hover states: border color change, transform
   - Selected state: accent color border

Follow the Lit implementation in `front-end/src/components/image/image-selector-modal.ts` for the grid layout.
Import type `StaticImage` from `@/gen/config_service_pb`.
  </action>
  <verify>
    <automated>cd front-end && npx vitest run src/react/components/files/FileList.test.tsx</automated>
  </verify>
  <done>
Component renders images in grid or list layout.
Each image shows thumbnail, name, and default badge.
Click on image triggers onImageSelect callback.
Selected image has distinctive styling.
Loading and empty states display correctly.
</done>
</task>

<task type="auto">
  <name>Task 3: Create ImageSelector modal component</name>
  <files>front-end/src/react/components/image/ImageSelector.tsx, front-end/src/react/components/image/ImageSelector.module.css</files>
  <action>
Create a React component `ImageSelector` that provides a modal for browsing and selecting images:

1. Component props:
   - `isOpen: boolean` - modal open state
   - `onClose: () => void` - callback when modal is closed
   - `onImageSelect: (image: StaticImage) => void` - callback when image is selected
   - `initialSelectedId?: string` - ID of initially selected image (optional)

2. State management:
   - `selectedImageId: string | null` - currently selected image in modal

3. UI structure:
   - Backdrop (semi-transparent black, fades in/out)
   - Modal container (centered, scales in)
   - Modal header:
     - Title: "Select Image"
     - Close button (×)
   - Modal content:
     - FileList component (grid layout)
     - Pass `images`, `selectedImageId`, `onImageSelect` to FileList

4. Animation:
   - Backdrop: opacity transition (0.3s ease)
   - Modal: transform scale + opacity transition (0.3s cubic-bezier)
   - Show/hide controlled by `isOpen` prop

5. Interaction:
   - Click backdrop: close modal
   - Click close button: close modal
   - Click image: call `onImageSelect(image)`, close modal
   - Escape key: close modal

6. Accessibility:
   - Trap focus within modal (optional, if time permits)
   - Close on Escape key
   - ARIA attributes (role="dialog", aria-modal="true")

7. Styling:
   - Use CSS Modules (`ImageSelector.module.css`)
   - Reuse CSS custom properties from Lit
   - Match Lit implementation in `front-end/src/components/flags/feature-flags-modal.ts` for modal pattern

Follow the Lit implementation in `front-end/src/components/image/image-selector-modal.ts` for functionality.
Use `useFiles` hook from Task 1 to fetch images.
Use `FileList` component from Task 2 for image display.
Import type `StaticImage` from `@/gen/config_service_pb`.
  </action>
  <verify>
    <automated>cd front-end && npx vitest run src/react/components/image/ImageSelector.test.tsx</automated>
  </verify>
  <done>
Modal renders with backdrop and container.
Modal shows/hides based on isOpen prop.
FileList displays images in modal.
Click image triggers onImageSelect and closes modal.
Click backdrop or close button closes modal.
Escape key closes modal.
Animations play correctly (fade, scale).
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → File List | Image URLs fetched from gRPC and displayed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | Image URLs | accept | URLs come from backend, already validated server-side |
| T-03-09 | Spoofing | Image selection | accept | Selection only affects UI, no backend mutation |
</threat_model>

<verification>
1. Run all Vitest tests: `cd front-end && npx vitest run src/react/hooks/useFiles.test.tsx src/react/components/files/FileList.test.tsx src/react/components/image/ImageSelector.test.tsx`
2. Verify TypeScript compilation: `cd front-end && npx tsc --noEmit`
3. Check component exports: `grep -r "export.*FileList\|export.*ImageSelector" front-end/src/react/components/`
4. Verify hook exports: `grep -r "export.*useFiles" front-end/src/react/hooks/`
5. Verify modal integration: `grep "FileList" front-end/src/react/components/image/ImageSelector.tsx`
</verification>

<success_criteria>
- FileList renders images in grid layout
- FileList displays thumbnails, names, and default badges
- FileList supports list and grid layouts
- Image selection works in FileList
- ImageSelector modal opens and closes correctly
- ImageSelector displays FileList with images
- Image selection in modal triggers callback and closes modal
- Backdrop and close button work
- Escape key closes modal
- Animations play correctly
- All tests pass
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-static-feature-ui/03-03-SUMMARY.md`
</output>
