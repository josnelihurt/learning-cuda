---
phase: 03-static-feature-ui
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - front-end/src/react/components/health/HealthIndicator.tsx
  - front-end/src/react/components/health/HealthPanel.tsx
  - front-end/src/react/main.tsx
autonomous: true
requirements:
  - HLTH-01
  - HLTH-02
user_setup: []

must_haves:
  truths:
    - "User sees a health status indicator in the UI"
    - "Health indicator shows visual state (green/red/orange)"
    - "Health indicator updates when backend status changes"
    - "User can see last check timestamp"
    - "User can see health status message"
    - "User receives visual feedback when backend becomes unavailable"
    - "Health panel displays detailed health information"
  artifacts:
    - path: "front-end/src/react/components/health/HealthIndicator.tsx"
      provides: "Compact health status indicator component"
      min_lines: 80
    - path: "front-end/src/react/components/health/HealthPanel.tsx"
      provides: "Detailed health information panel"
      min_lines: 100
  key_links:
    - from: "front-end/src/react/components/health/HealthIndicator.tsx"
      to: "front-end/src/react/hooks/useHealthMonitor.ts"
      via: "useHealthMonitor hook"
      pattern: "useHealthMonitor"
    - from: "front-end/src/react/components/health/HealthPanel.tsx"
      to: "front-end/src/react/hooks/useHealthMonitor.ts"
      via: "useHealthMonitor hook"
      pattern: "useHealthMonitor"
    - from: "front-end/src/react/main.tsx"
      to: "front-end/src/react/components/health/HealthIndicator.tsx"
      via: "Component mount in App"
      pattern: "HealthIndicator"
---

<objective>
Build health monitoring UI components that display backend health status with visual indicators and detailed information.

Purpose: Provide real-time health status visibility to users
Output: HealthIndicator component, HealthPanel component
</objective>

<execution_context>
@/home/jrb/code/cita-learning/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jrb/code/cuda-learning/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-static-feature-ui/03-RESEARCH.md
@.planning/phases/02-core-hook-infrastructure/02-02-SUMMARY.md

@front-end/src/react/hooks/useHealthMonitor.ts
@front-end/src/components/app/connection-status-card.ts
@front-end/src/components/app/grpc-unavailable.ts
@front-end/src/gen/remote_management_service_pb.ts

# Existing useHealthMonitor hook

From front-end/src/react/hooks/useHealthMonitor.ts:
```typescript
export function useHealthMonitor(options?: { pollIntervalMs?: number }) {
  // Returns:
  // - response: CheckAcceleratorHealthResponse | null
  // - isHealthy: boolean (derived from response.status === AcceleratorHealthStatus.HEALTHY)
  // - loading: boolean
  // - error: GrpcAsyncError | null
  // - lastChecked: Date | undefined
  // - (no refetch, polls automatically)
}
```

# Key types

From front-end/src/gen/remote_management_service_pb.ts:
```typescript
enum AcceleratorHealthStatus {
  HEALTHY = 0,
  UNHEALTHY = 1,
  UNKNOWN = 2,
}

class CheckAcceleratorHealthResponse {
  status: AcceleratorHealthStatus;
  message: string;
  traceContext?: TraceContext;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HealthIndicator component</name>
  <files>front-end/src/react/components/health/HealthIndicator.tsx, front-end/src/react/components/health/HealthIndicator.module.css</files>
  <action>
Create a React component `HealthIndicator` that displays a compact health status indicator:

1. Component props:
   - `isHealthy: boolean` - health status from useHealthMonitor
   - `loading?: boolean` - loading state (optional, default: false)
   - `onClick?: () => void` - click handler (optional, for expanding details)
   - `showLabel?: boolean` - whether to show text label (optional, default: true)

2. UI structure:
   - Indicator container (flex row, align center)
   - Status dot:
     - Green circle with glow when `isHealthy: true`
     - Red circle with glow when `isHealthy: false` and `!loading`
     - Orange circle with pulse animation when `loading: true`
   - Label text (if `showLabel: true`):
     - "Healthy" when `isHealthy: true`
     - "Unhealthy" when `isHealthy: false` and `!loading`
     - "Checking..." when `loading: true`
   - Tooltip (optional, on hover):
     - Show last check timestamp
     - Show status message if available

3. Status dot styling:
   - Size: 8-10px diameter
   - Border radius: 50%
   - Box shadow: glow effect matching color
   - Animation: pulse for loading state

4. Styling:
   - Use CSS Modules (`HealthIndicator.module.css`)
   - Reuse CSS custom properties from Lit
   - Status colors:
     - Healthy: green (#66ff66)
     - Unhealthy: red (#ff6666)
     - Loading: orange (#ffaa00)
   - Glow effects using box-shadow
   - Pulse animation for loading

Follow the Lit implementation in `front-end/src/components/app/connection-status-card.ts` for the status indicator pattern.
Use `useHealthMonitor` hook from `@/react/hooks/useHealthMonitor`.
  </action>
  <verify>
    <automated>cd front-end && npx vitest run src/react/components/health/HealthIndicator.test.tsx</automated>
  </verify>
  <done>
Component renders status dot with correct color.
Status dot has glow effect.
Label text displays correct status.
Pulse animation plays during loading state.
Click handler works if provided.
Tooltip shows on hover (if implemented).
</done>
</task>

<task type="auto">
  <name>Task 2: Create HealthPanel component</name>
  <files>front-end/src/react/components/health/HealthPanel.tsx, front-end/src/react/components/health/HealthPanel.module.css</files>
  <action>
Create a React component `HealthPanel` that displays detailed health information:

1. Component props:
   - `isHealthy: boolean` - health status from useHealthMonitor
   - `loading: boolean` - loading state
   - `error: GrpcAsyncError | null` - error message
   - `lastChecked: Date | undefined` - last check timestamp
   - `message?: string` - status message from response
   - `compact?: boolean` - compact mode (optional, default: false)

2. UI structure (normal mode):
   - Section header: "Backend Health"
   - Status row:
     - Large status icon (checkmark, warning, or spinner)
     - Status text: "Healthy" / "Unhealthy" / "Checking..."
   - Details section (when not loading):
     - Status message (if available)
     - Last checked: formatted timestamp
     - Error details (if error exists)
   - Refresh button (triggers manual check if hook supports it)

3. UI structure (compact mode):
   - Single row with status icon, status text, last checked
   - Expand on click to show details

4. Status icons:
   - Healthy: Green checkmark (✓)
   - Unhealthy: Red warning triangle (⚠)
   - Loading: Spinner animation

5. Timestamp formatting:
   - "Last checked: Just now" (within 1 minute)
   - "Last checked: X minutes ago" (within 1 hour)
   - "Last checked: HH:MM" (today)
   - "Last checked: YYYY-MM-DD HH:MM" (older)

6. Styling:
   - Use CSS Modules (`HealthPanel.module.css`)
   - Reuse CSS custom properties from Lit
   - Status colors match HealthIndicator
   - Card-like container with border
   - Responsive: full width on mobile, compact on desktop
   - Expand/collapse animation in compact mode

Follow the Lit implementation in `front-end/src/components/app/connection-status-card.ts` for the status display pattern.
Use `useHealthMonitor` hook from `@/react/hooks/useHealthMonitor`.
  </action>
  <verify>
    <automated>cd front-end && npx vitest run src/react/components/health/HealthPanel.test.tsx</automated>
  </verify>
  <done>
Component renders health status with correct icon and text.
Status message displays when available.
Last checked timestamp formats correctly.
Error details display when error exists.
Compact mode shows single row, expands on click.
Refresh button triggers manual check (if supported).
</done>
</task>

<task type="auto">
  <name>Task 3: Mount HealthIndicator in main App</name>
  <files>front-end/src/react/main.tsx, front-end/src/react/App.tsx</files>
  <action>
Integrate health monitoring UI into the main React application:

1. In `front-end/src/react/App.tsx`:
   - Import `useHealthMonitor` from `@/react/hooks/useHealthMonitor`
   - Import `HealthIndicator` from `@/react/components/health/HealthIndicator`
   - Add state: `const { isHealthy, loading } = useHealthMonitor();`
   - Add `HealthIndicator` to navbar or header area
   - Position: right side of navbar, before "React" badge
   - Pass props: `isHealthy={isHealthy}`, `loading={loading}`

2. Optional: Add HealthPanel to a dedicated section:
   - Create "Status" page or section
   - Mount `HealthPanel` component
   - Pass all props from `useHealthMonitor`

3. Verify positioning and styling match Lit frontend:
   - Health indicator in navbar
   - Visible at all times
   - Updates reactively

4. Ensure ServiceContext provider wraps the component tree (already in main.tsx from Phase 2).

Follow the Lit implementation in `front-end/src/components/app/app-root.ts` for health indicator positioning.
  </action>
  <verify>
    <automated>cd front-end && npm run build 2>&1 | grep -E "(error|warning|built)" | head -20</automated>
  </verify>
  <done>
HealthIndicator mounts in navbar.
HealthIndicator receives correct props from useHealthMonitor.
Health status updates reactively in UI.
Build succeeds without errors.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Health monitoring is read-only, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Information Disclosure | Health status | accept | Health status is non-sensitive information |
| T-03-14 | Spoofing | Health status | accept | Status comes from backend, already authenticated/trusted |
</threat_model>

<verification>
1. Run all Vitest tests: `cd front-end && npx vitest run src/react/components/health/HealthIndicator.test.tsx src/react/components/health/HealthPanel.test.tsx`
2. Verify TypeScript compilation: `cd front-end && npx tsc --noEmit`
3. Check component exports: `grep -r "export.*HealthIndicator\|export.*HealthPanel" front-end/src/react/components/`
4. Verify main.tsx integration: `grep "HealthIndicator\|useHealthMonitor" front-end/src/react/App.tsx`
5. Check useHealthMonitor usage: `grep "useHealthMonitor" front-end/src/react/components/health/`
</verification>

<success_criteria>
- HealthIndicator renders status dot with correct color
- HealthIndicator displays label text
- HealthIndicator pulse animation plays during loading
- HealthPanel displays detailed health information
- HealthPanel shows status message, timestamp, and errors
- HealthPanel compact mode works
- HealthIndicator mounts in navbar
- Health status updates reactively
- All tests pass
- TypeScript compilation succeeds
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-static-feature-ui/03-05-SUMMARY.md`
</output>
