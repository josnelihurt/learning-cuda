---
phase: 05-polish-and-parity-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - front-end/package.json
  - front-end/vite.config.ts
  - front-end/tsconfig.json
  - front-end/tsconfig.base.json
  - front-end/tests/e2e/dual-frontend-routes.spec.ts
  - front-end/tests/e2e/helpers/console-errors.ts
autonomous: true
requirements:
  - PAR-01
user_setup: []

must_haves:
  truths:
    - "`cd front-end && npx tsc --noEmit` exits 0 (strict project typecheck independent of Vite)"
    - "`cd front-end && npm run build` completes successfully after type fixes"
    - "Playwright asserts no browser `console` messages with `type() === 'error'` on first navigation to `/react` and `/lit` under the preview harness (or documented equivalent command)"
    - "Package.json exposes an explicit typecheck entry point so CI/humans cannot confuse `vite build` with semantic TS validation"
  artifacts:
    - path: "front-end/package.json"
      provides: "npm script for TypeScript noEmit (e.g. typecheck)"
      contains: "tsc --noEmit"
    - path: "front-end/tests/e2e/helpers/console-errors.ts"
      provides: "Reusable Playwright console error collector / assertion"
      min_lines: 15
  key_links:
    - from: "front-end/package.json"
      to: "tsc compiler"
      via: "typecheck script must not be satisfied by vite build alone"
      pattern: "\"build\".*vite|tsc --noEmit"
---

<objective>
Establish an automated **build validation gate** for Phase 5: TypeScript strict checking via `tsc --noEmit`, a successful Vite production build, and a **console error policy** enforced with Playwright hooks — supporting PAR-01 by ensuring the React/Lit shells are type-safe and load without console errors before manual parity work.

Purpose: Vite transpiles without running the TypeScript type checker; roadmap success criteria require zero strict-mode errors and no console errors on load — this plan encodes those gates without treating `npm run build` as a substitute for `tsc` (per `05-RESEARCH.md`).

Output: Clean `tsc --noEmit`, `npm run build`, and Playwright coverage for console errors on `/react` and `/lit`.
</objective>

<execution_context>
@/home/jrb/code/cuda-learning/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-polish-and-parity-validation/05-RESEARCH.md
@.planning/phases/05-polish-and-parity-validation/05-VALIDATION.md
@front-end/vite.config.ts
@front-end/tsconfig.base.json
@front-end/tsconfig.json
@front-end/playwright.config.ts

**Prerequisite (phase scope):** Phase 4 (Video Streaming and WebRTC) should be complete before executing this plan, so WebRTC-related sources are stable under `tsc` and preview. This plan does not weaken production TLS or security settings; Playwright may use existing dev/test TLS ignore patterns only in test config.

**Research fact (non-negotiable):** `vite build` does **not** replace `npx tsc --noEmit`. Fix typing boundaries until `tsc` is clean; do not “fix” by removing strictness globally.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TypeScript strict project until `tsc --noEmit` passes</name>
  <files>front-end/vite.config.ts, front-end/tsconfig.json, front-end/tsconfig.base.json, plus any TS sources surfaced by tsc</files>
  <read_first>
Read `front-end/tsconfig.json` and `front-end/tsconfig.base.json` to see which files are included in the typecheck graph. Read `front-end/vite.config.ts` fully. Run `cd /home/jrb/code/cuda-learning/front-end && npx tsc --noEmit` once and capture the full error list (research notes cite `preview.https` vs `UserConfig` as a known starter failure).
  </read_first>
  <action>
Resolve every `tsc` diagnostic until `cd /home/jrb/code/cuda-learning/front-end && npx tsc --noEmit` exits with code 0. Prefer minimal, type-correct fixes: narrow types (`satisfies`, explicit interfaces), adjust `vite.config.ts` options to match Vite 5 typings, or use targeted `as const` / helper types — **do not** disable `strict` or broadly suppress errors. If a dependency type forces friction, fix at the boundary (wrapper type, `satisfies UserConfig`, or narrow import) rather than loosening `tsconfig` for the whole app. Document in the commit/summary if any fix touches shared config so Phase 5 follow-on plans stay aware.
  </action>
  <acceptance_criteria>
`cd /home/jrb/code/cuda-learning/front-end && npx tsc --noEmit` exits 0. Grep confirms `tsconfig.base.json` still contains `"strict": true` (or stricter equivalent). No new `// @ts-ignore` without a one-line rationale comment next to it (prefer zero suppressions).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jrb/code/cuda-learning/front-end && npx tsc --noEmit</automated>
  </verify>
  <done>Strict project typecheck exits 0; strict flag remains enabled in tsconfig.base.json.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit `typecheck` script and document Vite vs tsc</name>
  <files>front-end/package.json</files>
  <read_first>
Read `front-end/package.json` scripts section. Confirm no existing script already runs `tsc --noEmit` (research: none required today).
  </read_first>
  <action>
Add an npm script (name `typecheck` or `check:types`) that runs `tsc --noEmit` from `front-end/` with no watch mode. In the plan executor summary (SUMMARY.md), add one short bullet stating that **`npm run build` validates bundling only** and **`npm run typecheck` (or `npx tsc --noEmit`) validates strict types** — mirrors `05-RESEARCH.md` and Vite documentation. Do not remove or rename `build`.
  </action>
  <acceptance_criteria>
`cd /home/jrb/code/cuda-learning/front-end && npm run typecheck` (or the chosen script name) exits 0 and invokes `tsc --noEmit`. `grep -E 'tsc --noEmit|typecheck' front-end/package.json` shows the script. `npm run build` still exists unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jrb/code/cuda-learning/front-end && npm run typecheck && grep -E 'tsc --noEmit|typecheck' package.json</automated>
  </verify>
  <done>package.json exposes a script that runs `tsc --noEmit`; `npm run build` unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Playwright console error policy for `/react` and `/lit` loads</name>
  <files>front-end/tests/e2e/helpers/console-errors.ts, front-end/tests/e2e/dual-frontend-routes.spec.ts</files>
  <read_first>
Read `front-end/tests/e2e/dual-frontend-routes.spec.ts` and `front-end/playwright.config.ts` (baseURL, preview command). Read `05-RESEARCH.md` “Pattern 2: Console error budget” for filtering **error** only (not `warning`).
  </read_first>
  <action>
Create `front-end/tests/e2e/helpers/console-errors.ts` exporting a small helper that attaches `page.on('console')` before `goto`, collects `msg.type() === 'error'`, and throws with message text on failure (no empty catch — log or throw with text). Update `dual-frontend-routes.spec.ts` to use this helper for both `/react` and `/lit` navigations so first-load console **errors** fail the test. Keep scope to **errors** only to avoid flaky warnings (per research). Do not add `ignoreHTTPSErrors` globally for production paths; use existing Playwright test config patterns only. If console errors require app fixes (e.g. thrown in React mount), fix the underlying issue rather than silencing the test.
  </action>
  <acceptance_criteria>
`cd /home/jrb/code/cuda-learning/front-end && npm run test:e2e -- tests/e2e/dual-frontend-routes.spec.ts` passes. `cd /home/jrb/code/cuda-learning/front-end && npm run build` exits 0 after Task 1–2. Grep shows `page.on('console'` and `msg.type()` or equivalent in the helper file.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jrb/code/cuda-learning/front-end && npm run build && npm run test:e2e -- tests/e2e/dual-frontend-routes.spec.ts && grep -q "page.on('console'" tests/e2e/helpers/console-errors.ts</automated>
  </verify>
  <done>Playwright dual-route spec passes with console error policy; helper file exists and is used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine / CI → front-end repo | Typecheck and tests run on source; must not introduce unsafe patterns to “pass” types |
| Browser → app (PAR-01 prep) | Console policy catches runtime failures that could indicate XSS or broken integrity of UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `vite.config.ts` / TLS-related options | mitigate | Fix types without disabling HTTPS in production; dev-only TLS quirks stay in dev server / Playwright config, not shipped nginx config |
| T-05-01-02 | Information disclosure | Console error messages in tests | accept | Error text may leak stack traces in CI logs; low sensitivity for this app — keep messages for debugging |
| T-05-01-03 | Elevation of privilege | Weakening `strict` or blanket `@ts-ignore` | mitigate | Resolve root causes; document rare suppressions — do not turn off strict mode to greenlight build |
</threat_model>

<verification>
- `cd /home/jrb/code/cuda-learning/front-end && npx tsc --noEmit`
- `cd /home/jrb/code/cuda-learning/front-end && npm run build`
- `cd /home/jrb/code/cuda-learning/front-end && npm run test:e2e -- tests/e2e/dual-frontend-routes.spec.ts`
</verification>

<success_criteria>
Typecheck and production build are both green; dual-route Playwright spec fails on browser console **error** during shell load; PAR-01 manual work can proceed on a stable, type-checked frontend.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-parity-validation/05-01-SUMMARY.md` per GSD workflow.
</output>
