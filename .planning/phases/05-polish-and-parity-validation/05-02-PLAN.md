---
phase: 05-polish-and-parity-validation
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - front-end/src/react/**/*.module.css
  - front-end/src/styles/**/*.css
  - .planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md
autonomous: true
requirements:
  - PAR-01
user_setup: []

must_haves:
  truths:
    - "Audit compares Lit global/token sources vs React `var(--*)` usage (documented in parity report)"
    - "Hardcoded colors/spacing in React CSS modules reduced or justified with references to shared tokens"
    - "`05-02-CSS-PARITY-REPORT.md` exists with findings, fixes, and visual verification notes"
  artifacts:
    - path: ".planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md"
      provides: "CSS parity audit vs Lit, remediation list, manual visual verification checklist"
      min_lines: 40
  key_links:
    - from: "front-end/src/react/**/*.module.css"
      to: "Lit global styles / token definitions"
      via: "shared `var(--token)` alignment"
      pattern: "var\\(--"
---

<objective>
Deliver **CSS parity** between the Lit and React routes by auditing **design tokens / custom properties**, replacing inappropriate hardcoded values in React CSS modules, and recording a **parity report** with visual verification guidance — directly supporting PAR-01 success criterion #3 (no layout gaps or missing styles vs Lit).

Purpose: Functional parity (PAR-01) includes visual alignment; token drift causes subtle regressions that automated tests may miss.

Output: Updated CSS modules where needed, plus `05-02-CSS-PARITY-REPORT.md` in the canonical phase folder.
</objective>

<execution_context>
@/home/jrb/code/cuda-learning/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-polish-and-parity-validation/05-RESEARCH.md

**Depends on:** Plan `05-01` (build/typecheck/console gate) so stylesheet edits are validated under the same strict pipeline. **Phase 4** should be complete before Phase 5 execution so video/WebRTC UI styles are in scope for audit.

**Scope note:** PAR-01 distinguishes **functional** identity from **pixel-perfect** diff automation; this plan targets roadmap “CSS rendering matches” via tokens and manual side-by-side notes — not Percy unless later adopted.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token and custom-property audit (Lit vs React)</name>
  <files>front-end/src/styles/**/*.css, front-end/src/components/**/*.css, front-end/src/react/**/*.module.css, .planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md</files>
  <read_first>
Scan Lit-side global styles: list files under `front-end/src/styles/` and root Lit CSS that define `--*` tokens. Grep React modules: `rg "var\\(--" front-end/src/react` and `rg "#[0-9a-fA-F]{3,8}|rgb\\(|rgba\\(" front-end/src/react --glob "*.module.css"`. Read `05-RESEARCH.md` pitfall “CSS token drift”.
  </read_first>
  <action>
Produce the first section of `05-02-CSS-PARITY-REPORT.md`: a table listing **token name**, **Lit definition location** (file path), **React usage** (file paths or “missing”), and **drift** (match / wrong fallback / hardcoded duplicate). Include instructions for how you searched (grep patterns) so the audit is reproducible. Do not change production security headers or content policies — CSS only.
  </action>
  <acceptance_criteria>
File `/home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md` exists and contains an audit table with columns for token, Lit source, React consumers, and status (`test -f` plus `grep -Ei "token|lit|react"`).
  </acceptance_criteria>
  <verify>
    <automated>test -f /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md && grep -Ei "token|lit|react" /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md | head -5</automated>
  </verify>
  <done>CSS parity report file exists with a token audit table and reproducible search notes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded values and align `var(--*)` usage</name>
  <files>front-end/src/react/**/*.module.css (subset per audit)</files>
  <read_first>
Read the audit section of `05-02-CSS-PARITY-REPORT.md` and open only the React module files flagged as drift or hardcoded.
  </read_first>
  <action>
For each high-impact drift row, update React CSS modules to use the same custom properties as Lit (with fallbacks only where already idiomatic in repo). Prefer deleting duplicate hex values in favor of `var(--token)`; if a token is missing on the Lit side, add it to the **shared** global style file (not per-component random hex) only if that matches existing Lit design — otherwise document “Lit also hardcoded” in the report and match Lit literally. Keep changes limited to presentation (no behavior/React logic in this task unless required for class names). Run `cd /home/jrb/code/cuda-learning/front-end && npm run typecheck` and `npm run build` after edits.
  </action>
  <acceptance_criteria>
`cd /home/jrb/code/cuda-learning/front-end && npm run typecheck` exits 0. `cd /home/jrb/code/cuda-learning/front-end && npm run build` exits 0. Report appendix lists files changed and before/after notes for each material token fix.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jrb/code/cuda-learning/front-end && npm run typecheck && npm run build</automated>
  </verify>
  <done>React CSS modules updated per audit; typecheck and build green; report appendix updated.</done>
</task>

<task type="auto">
  <name>Task 3: Visual verification notes and parity sign-off section</name>
  <files>.planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md</files>
  <read_first>
Read ROADMAP Phase 5 success criteria #1 and #3. Read completed Task 1–2 sections in `05-02-CSS-PARITY-REPORT.md`.
  </read_first>
  <action>
Append to `05-02-CSS-PARITY-REPORT.md`: **Visual verification protocol** — open `/react` and `/lit` side-by-side at desktop width (e.g. 1440px), list **views to compare** (shell, image area, file list, settings, health, video area post–Phase 4), and for each note **what to look for** (spacing, typography, focus rings, error states). State explicitly that pixel-perfect OS font differences may occur; record “acceptable” vs “bug”. This supports PAR-01 manual method without claiming automated screenshot parity.
  </action>
  <acceptance_criteria>
`grep -q "Visual verification" /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md` exits 0. Document mentions both `/react` and `/lit`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Visual verification" /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md && grep -E "/react|/lit" /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md</automated>
  </verify>
  <done>Report includes visual verification protocol covering `/react` and `/lit`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → browser | Styles must not inject untrusted `content:` URLs or `behavior` (IE); stay within normal CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Spoofing / XSS | `content` property, `url()` in CSS | mitigate | Do not embed user-controlled strings in CSS; tokens only from static stylesheets |
| T-05-02-02 | Tampering | N/A for CSS audit | accept | No security config changes in this plan |
| T-05-02-03 | Denial of service | Massive CSS imports | accept | Keep edits minimal; no import bombs |
</threat_model>

<verification>
- `cd /home/jrb/code/cuda-learning/front-end && npm run typecheck`
- `cd /home/jrb/code/cuda-learning/front-end && npm run build`
- `test -f /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-02-CSS-PARITY-REPORT.md`
</verification>

<success_criteria>
Report artifact exists; token audit and remediation are documented; React build stays green — enabling trustworthy visual PAR-01 review.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-parity-validation/05-02-SUMMARY.md`.
</output>
