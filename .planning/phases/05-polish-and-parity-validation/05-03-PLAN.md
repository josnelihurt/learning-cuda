---
phase: 05-polish-and-parity-validation
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - .planning/phases/05-polish-and-parity-validation/PAR-01-CHECKLIST.md
  - .planning/phases/05-polish-and-parity-validation/05-03-FEATURE-PARITY-PROCEDURE.md
  - .planning/phases/05-polish-and-parity-validation/05-03-GAP-ANALYSIS.md
  - front-end/tests/e2e/parity-smoke.spec.ts
autonomous: false
requirements:
  - PAR-01
user_setup: []

must_haves:
  truths:
    - "`PAR-01-CHECKLIST.md` lists every v1.0 feature area (IMG/FILE/CONF/HLTH/VID) with pass/fail columns for Lit and React"
    - "`05-03-FEATURE-PARITY-PROCEDURE.md` documents side-by-side `/react` vs `/lit` steps and environment (VITE_API_ORIGIN, backend)"
    - "`05-03-GAP-ANALYSIS.md` records discrepancies and whether they block PAR-01"
  artifacts:
    - path: ".planning/phases/05-polish-and-parity-validation/PAR-01-CHECKLIST.md"
      provides: "Executable PAR-01 checklist"
      min_lines: 35
    - path: ".planning/phases/05-polish-and-parity-validation/05-03-FEATURE-PARITY-PROCEDURE.md"
      provides: "Side-by-side /lit vs /react testing procedure"
      min_lines: 25
    - path: ".planning/phases/05-polish-and-parity-validation/05-03-GAP-ANALYSIS.md"
      provides: "Gap analysis and resolutions"
      min_lines: 25
  key_links:
    - from: "PAR-01-CHECKLIST.md"
      to: "REQUIREMENTS v1.0 feature IDs"
      via: "trace rows to IMG/FILE/CONF/HLTH/VID"
      pattern: "IMG-|FILE-|CONF-|HLTH-|VID-"
---

<objective>
Complete **feature parity validation** for PAR-01: a **checklist** covering all v1.0 features, a **written side-by-side testing procedure** for `/react` vs `/lit`, and a **gap analysis report** — with optional lightweight Playwright smoke to assist (not replace) manual verification.

Purpose: REQUIREMENTS **PAR-01** is explicitly **manual** developer verification; automation supplements route shells and regressions, but human sign-off remains authoritative.

Output: `PAR-01-CHECKLIST.md`, `05-03-FEATURE-PARITY-PROCEDURE.md`, `05-03-GAP-ANALYSIS.md`, and optional `parity-smoke.spec.ts` if it adds clear value without flaking.
</objective>

<execution_context>
@/home/jrb/code/cuda-learning/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-parity-validation/05-RESEARCH.md
@front-end/tests/e2e/dual-frontend-routes.spec.ts

**Depends on:** Plan `05-02` (CSS/token work) so visual comparison during PAR-01 is meaningful; **Plan `05-01`** is transitively required. **Phase 4** must be complete so VID-* rows are testable.

**Canonical artifacts only:** All new markdown lives under `.planning/phases/05-polish-and-parity-validation/` (do not write parallel drafts under other phase folders).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author PAR-01 checklist (requirements-traceable)</name>
  <files>.planning/phases/05-polish-and-parity-validation/PAR-01-CHECKLIST.md</files>
  <read_first>
Read `.planning/REQUIREMENTS.md` sections Image Processing, File Management, Settings, Health Monitoring, Video Streaming — extract requirement IDs **IMG-***, **FILE-***, **CONF-***, **HLTH-***, **VID-***. Read ROADMAP Phase 5 success criterion #1.
  </read_first>
  <action>
Create `PAR-01-CHECKLIST.md` with one row per requirement ID (and sensible sub-rows for multi-step flows, e.g. upload → filter → result). Columns: **Requirement ID**, **Scenario**, **Lit (/lit) — Pass?**, **React (/react) — Pass?**, **Notes / evidence**. Include a preamble: backend must be reachable per dev topology (`VITE_API_ORIGIN`, Go TLS) — reference `CLAUDE.md` / `front-end` env docs without duplicating long prose. Do not claim automated equivalence; this is a manual record.
  </action>
  <acceptance_criteria>
`grep -E "IMG-|FILE-|CONF-|HLTH-|VID-" /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/PAR-01-CHECKLIST.md` returns at least one line per family. File exists at the exact path above.
  </acceptance_criteria>
  <verify>
    <automated>test -f /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/PAR-01-CHECKLIST.md && grep -E "IMG-|FILE-|CONF-|HLTH-|VID-" /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/PAR-01-CHECKLIST.md | head -20</automated>
  </verify>
  <done>PAR-01 checklist exists with traceability to all requirement ID families.</done>
</task>

<task type="auto">
  <name>Task 2: Side-by-side testing procedure document</name>
  <files>.planning/phases/05-polish-and-parity-validation/05-03-FEATURE-PARITY-PROCEDURE.md</files>
  <read_first>
Read `05-RESEARCH.md` “Pattern 1: Dual-route verification” and “Environment Availability”. Read `front-end/playwright.config.ts` for default `baseURL` / preview behavior.
  </read_first>
  <action>
Write `05-03-FEATURE-PARITY-PROCEDURE.md` with numbered steps: (1) start dev stack per `CLAUDE.md` (`./scripts/dev/start.sh` or documented equivalent), (2) open two browser windows **same viewport** — navigate to `https://localhost:8443` or the documented UI origin with **`/lit` and `/react`** paths per project routing (adjust host/port to match repo; Vite 3000 vs Go 8443 — state explicitly to run UI where MPA is served in dev), (3) for each checklist row execute the **same user actions** on both routes, (4) record outcomes in `PAR-01-CHECKLIST.md`, (5) if mismatch, file entry in gap analysis (next task). Include **security note**: do not disable TLS verification in production; dev-only certificate trust is operator setup. Reference `npm run test:e2e` only as supplementary.
  </action>
  <acceptance_criteria>
`grep -E "/react|/lit" /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-03-FEATURE-PARITY-PROCEDURE.md` finds both paths. File length ≥ 25 lines or contains at least 8 numbered steps.
  </acceptance_criteria>
  <verify>
    <automated>test -f /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-03-FEATURE-PARITY-PROCEDURE.md && grep -E "/react|/lit" /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-03-FEATURE-PARITY-PROCEDURE.md && wc -l /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-03-FEATURE-PARITY-PROCEDURE.md</automated>
  </verify>
  <done>Side-by-side procedure document exists with explicit `/react` and `/lit` steps.</done>
</task>

<task type="auto">
  <name>Task 3: Gap analysis report + optional parity smoke spec</name>
  <files>.planning/phases/05-polish-and-parity-validation/05-03-GAP-ANALYSIS.md, front-end/tests/e2e/parity-smoke.spec.ts</files>
  <read_first>
Read `PAR-01-CHECKLIST.md` (draft from Task 1) and `05-02-CSS-PARITY-REPORT.md` if present for cross-links. Read `dual-frontend-routes.spec.ts` to mirror imports/style.
  </read_first>
  <action>
Create `05-03-GAP-ANALYSIS.md` with sections: **Summary**, **Open gaps** (requirement ID, description, severity Blocker / Minor, owner suggestion), **Resolved in phase** (if any), **Residual risk**. Optionally add `front-end/tests/e2e/parity-smoke.spec.ts` that only asserts both shells load and critical landmarks exist (reuse console helper from `05-01` if available) — **do not** try to fully automate PAR-01. If the spec would duplicate `dual-frontend-routes.spec.ts` without value, omit the file and state “no new spec” in the gap doc.

If any gap requires backend changes, note **Out of scope per REQUIREMENTS** and do not propose backend edits.
  </action>
  <acceptance_criteria>
`test -f /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-03-GAP-ANALYSIS.md` exits 0. If `parity-smoke.spec.ts` exists: `cd /home/jrb/code/cuda-learning/front-end && npm run test:e2e -- tests/e2e/parity-smoke.spec.ts` passes. If spec omitted: gap doc states why. `cd /home/jrb/code/cuda-learning/front-end && npm run typecheck` still passes.
  </acceptance_criteria>
  <verify>
    <automated>test -f /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-03-GAP-ANALYSIS.md && cd /home/jrb/code/cuda-learning/front-end && npm run typecheck && (test ! -f tests/e2e/parity-smoke.spec.ts || npm run test:e2e -- tests/e2e/parity-smoke.spec.ts)</automated>
  </verify>
  <done>Gap analysis exists; optional e2e spec passes or omission is documented with rationale.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human PAR-01 sign-off</name>
  <files>.planning/phases/05-polish-and-parity-validation/PAR-01-CHECKLIST.md, .planning/phases/05-polish-and-parity-validation/05-03-FEATURE-PARITY-PROCEDURE.md, .planning/phases/05-polish-and-parity-validation/05-03-GAP-ANALYSIS.md</files>
  <read_first>
Read `05-03-FEATURE-PARITY-PROCEDURE.md`, `PAR-01-CHECKLIST.md`, and `05-03-GAP-ANALYSIS.md`. Ensure dev stack and TLS trust match project docs before testing.
  </read_first>
  <action>
Operator runs the full manual PAR-01 pass: for each checklist row, perform the same actions on `/lit` and `/react`, record Pass/Fail and notes, and update `05-03-GAP-ANALYSIS.md` with any discrepancies. Run automated pre-check: `cd /home/jrb/code/cuda-learning/front-end && npm run test:e2e -- tests/e2e/dual-frontend-routes.spec.ts` (console policy from Plan 05-01). This checkpoint cannot be automated away — it satisfies REQUIREMENTS **PAR-01** wording.
  </action>
  <acceptance_criteria>
Checklist has been exercised or explicitly marked N/A with reason; gap analysis reflects current reality; operator replies with `approved` or lists follow-up gaps.
  </acceptance_criteria>
  <what-built>Checklist, procedure, and gap analysis artifacts for manual `/lit` vs `/react` parity validation after Plans 05-01–02 automation gates.</what-built>
  <how-to-verify>
1. With backend running, execute the procedure in `05-03-FEATURE-PARITY-PROCEDURE.md` for each checklist row.
2. Fill Pass/Fail in `PAR-01-CHECKLIST.md` honestly; update `05-03-GAP-ANALYSIS.md` with any remaining gaps.
3. Confirm no console **errors** on shell load (Playwright gate from 05-01) still passes: `cd /home/jrb/code/cuda-learning/front-end && npm run test:e2e -- tests/e2e/dual-frontend-routes.spec.ts`.
  </how-to-verify>
  <verify>
    <automated>cd /home/jrb/code/cuda-learning/front-end && npm run test:e2e -- tests/e2e/dual-frontend-routes.spec.ts</automated>
  </verify>
  <done>Human confirmed PAR-01 manual verification complete or documented blocking gaps.</done>
  <resume-signal>Type `approved` or list gaps to address in a follow-up plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → dev environment | Procedure must not instruct disabling TLS for real deployments |
| Tester → app | Manual testing exercises real APIs — no weakening of CORS or auth (N/A today) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Spoofing | Phishing via wrong URL in procedure | mitigate | Procedure uses exact `/react` and `/lit` paths and documented host from repo |
| T-05-03-02 | Tampering | “Fix” gaps by unsafe HTML | mitigate | Gap resolutions stay in React/Lit UI patterns — no `dangerouslySetInnerHTML` for user content |
| T-05-03-03 | Information disclosure | Gap doc leaks secrets | mitigate | No API keys or paths to private files in templates; notes are functional only |
</threat_model>

<verification>
- `cd /home/jrb/code/cuda-learning/front-end && npm run typecheck`
- `cd /home/jrb/code/cuda-learning/front-end && npm run test:e2e -- tests/e2e/dual-frontend-routes.spec.ts`
- `ls /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/PAR-01-CHECKLIST.md /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-03-FEATURE-PARITY-PROCEDURE.md /home/jrb/code/cuda-learning/.planning/phases/05-polish-and-parity-validation/05-03-GAP-ANALYSIS.md`
</verification>

<success_criteria>
PAR-01 artifacts exist and are requirement-traceable; human checkpoint captures final acceptance; gaps are explicit and scoped to frontend parity.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-parity-validation/05-03-SUMMARY.md`.
</output>
