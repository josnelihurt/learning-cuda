---
phase: 05-polish-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - front-end/tsconfig.json
  - front-end/src/react/**/*.tsx
  - front-end/src/react/**/*.ts
autonomous: true
requirements:
  - PAR-01
user_setup: []

must_haves:
  truths:
    - React production build completes with zero TypeScript strict-mode errors
    - React frontend loads in browser with zero console errors
    - All React components compile without type errors
    - Build artifacts are generated correctly in dist/
  artifacts:
    - path: front-end/dist/
      provides: Production build output
      contains: react.html, react-*.js, react-*.css
    - path: front-end/tsconfig.json
      provides: TypeScript configuration
      contains: strict mode enabled
  key_links:
    - from: npm run build
      to: dist/react.html
      via: Vite build process
      pattern: vite build
    - from: Browser console
      to: React application
      via: Page load
      pattern: console.error
---

<objective>
Validate React production build and runtime with zero errors

Purpose: Ensure the React frontend meets production quality standards with no TypeScript strict-mode errors and no console errors on page load. This is Success Criteria #2 from Phase 5.

Output: Validated production build with clean console
</objective>

<execution_context>
@/home/jrb/code/cuda-learning/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jrb/code/cuda-learning/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@/home/jrb/code/cuda-learning/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify TypeScript strict mode configuration</name>
  <files>front-end/tsconfig.json</files>
  <read_first>
    - front-end/tsconfig.json
  </read_first>
  <action>
    Verify that tsconfig.json has strict mode enabled:
    - Check that `"strict": true` is present in compilerOptions
    - Check that no `"skipLibCheck": true` is present (should check all types)
    - Ensure target is set to "ES2020" or higher
    - Ensure module is set to "ESNext"
    If strict mode is not enabled, add `"strict": true` to compilerOptions.
  </action>
  <verify>
    <automated>grep '"strict".*:.*true' front-end/tsconfig.json</automated>
  </verify>
  <done>
    tsconfig.json contains "strict": true in compilerOptions
  </done>
</task>

<task type="auto">
  <name>Task 2: Run production build and check for TypeScript errors</name>
  <files>front-end/dist/</files>
  <read_first>
    - front-end/tsconfig.json
    - front-end/vite.config.ts
  </read_first>
  <action>
    Run production build and capture output:
    1. Clean existing build: rm -rf front-end/dist/
    2. Run build: cd front-end && npm run build
    3. Check exit code - must be 0
    4. Check output for any TypeScript errors (lines containing "error TS")
    5. Verify build artifacts are created:
       - dist/react.html exists
       - dist/assets/react-*.js exists
       - dist/assets/react-*.css exists
  </action>
  <verify>
    <automated>cd front-end && npm run build 2>&1 | tee /tmp/build-output.txt && ! grep -q "error TS" /tmp/build-output.txt && test -f dist/react.html && test -f dist/assets/react-*.js</automated>
  </verify>
  <done>
    Production build completes with exit code 0, zero TypeScript errors, and all required artifacts present
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify console error check setup</name>
  <files>.planning/phases/05-polish-parity/console-check-instructions.md</files>
  <read_first>
    - front-end/src/react/main.tsx
    - front-end/src/react/App.tsx
  </read_first>
  <action>
    Create console error verification instructions document:
    1. Create .planning/phases/05-polish-parity/console-check-instructions.md
    2. Document steps to verify zero console errors:
       - Start dev server: cd front-end && npm run dev
       - Open https://localhost:3000/react in browser
       - Open browser DevTools Console tab
       - Reload page (Cmd+R / F5)
       - Check for any red error messages
       - Clear console and navigate to each major feature route
       - Verify no errors on navigation
    3. Note that warnings are acceptable but errors must be zero
  </action>
  <verify>
    <automated>test -f .planning/phases/05-polish-parity/console-check-instructions.md && grep -q "console error" .planning/phases/05-polish-parity/console-check-instructions.md</automated>
  </verify>
  <done>
    Console check instructions document exists with clear steps for verifying zero console errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output | Build process generates artifacts consumed by browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | TypeScript compiler | mitigate | Strict mode enabled in tsconfig.json catches type vulnerabilities at build time |
| T-05-02 | I | Runtime console errors | mitigate | Verification instructions ensure errors are detected before production deployment |
</threat_model>

<verification>
1. Production build completes with exit code 0
2. No TypeScript errors in build output (grep for "error TS" returns nothing)
3. Build artifacts present: react.html, react-*.js, react-*.css
4. Console check instructions document created
5. Follow console check instructions to verify zero runtime errors
</verification>

<success_criteria>
- Zero TypeScript strict-mode errors in production build
- Zero console errors on page load
- All build artifacts generated correctly
- Console verification documented and executable
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-parity/05-01-SUMMARY.md`
</output>
