---
phase: 05-polish-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - front-end/src/react/**/*.module.css
  - front-end/src/components/**/*.css
  - .planning/phases/05-polish-parity/css-parity-report.md
autonomous: false
requirements:
  - PAR-01
user_setup: []

must_haves:
  truths:
    - React frontend CSS rendering matches Lit frontend visually
    - No layout gaps or missing styles in React version
    - CSS custom properties (variables) are used consistently
    - Responsive behavior matches between both frontends
  artifacts:
    - path: front-end/src/react/**/*.module.css
      provides: React component styles
      contains: CSS using custom properties from global styles
    - path: .planning/phases/05-polish-parity/css-parity-report.md
      provides: Visual comparison report
      contains: Screenshot comparisons, identified gaps, fixes applied
  key_links:
    - from: React components
      to: Global CSS variables
      via: CSS custom properties
      pattern: var\(--[a-z-]+\)
    - from: Lit components
      to: Global CSS variables
      via: CSS custom properties
      pattern: var\(--[a-z-]+\)
</key_links>
---

<objective>
Verify CSS and visual parity between React and Lit frontends

Purpose: Ensure the React frontend's CSS rendering matches the Lit frontend with no layout gaps or missing styles. This is Success Criteria #3 from Phase 5.

Output: CSS parity report with identified gaps and fixes
</objective>

<execution_context>
@/home/jrb/code/cuda-learning/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jrb/code/cuda-learning/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@/home/jrb/code/cuda-learning/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit CSS custom property usage</name>
  <files>front-end/src/react/**/*.module.css</files>
  <read_first>
    - front-end/src/react/components/image/ImageProcessor.module.css
    - front-end/src/react/components/filters/FilterPanel.module.css
    - front-end/src/react/components/health/HealthIndicator.module.css
  </read_first>
  <action>
    Audit all React CSS modules for CSS custom property usage:
    1. Search for all .module.css files in front-end/src/react/
    2. Extract all var(--variable-name) usage patterns
    3. Verify these variables are defined in global CSS (front-end/src/styles/ or equivalent)
    4. Check for any hardcoded colors, spacing, or font sizes that should use variables
    5. Generate list of:
       - Variables used in React
       - Variables used in Lit (from front-end/src/components/**/*.css)
       - Missing variables (used but not defined)
       - Hardcoded values that should be variables
    Save findings to .planning/phases/05-polish-parity/css-audit-findings.md
  </action>
  <verify>
    <automated>test -f .planning/phases/05-polish-parity/css-audit-findings.md && grep -q "CSS custom property" .planning/phases/05-polish-parity/css-audit-findings.md</automated>
  </verify>
  <done>
    CSS audit findings document exists with complete variable usage analysis
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix missing CSS variables and hardcoded values</name>
  <files>front-end/src/react/**/*.module.css</files>
  <read_first>
    - .planning/phases/05-polish-parity/css-audit-findings.md
  </read_first>
  <action>
    Based on audit findings, fix CSS issues:
    1. For each missing variable: Add definition to global CSS file
    2. For each hardcoded value: Replace with appropriate CSS custom property
    3. Ensure consistent naming: Use kebab-case for variables (e.g., --color-primary, --spacing-md)
    4. Match variable values to Lit frontend's global CSS
    5. Test that React build still completes after changes
  </action>
  <verify>
    <automated>cd front-end && npm run build 2>&1 | ! grep -q "error TS"</automated>
  </verify>
  <done>
    All CSS issues identified in audit are fixed, build completes successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CSS audit completed, issues fixed, ready for visual verification</what-built>
  <how-to-verify>
    Visual parity verification steps:
    1. Start dev server: cd front-end && npm run dev
    2. Open two browser windows side-by-side:
       - Window 1: https://localhost:3000/lit
       - Window 2: https://localhost:3000/react
    3. Compare visual elements:
       - Image upload area (drag-drop zone)
       - Filter panel (selection, parameters, drag-reorder)
       - Image processor (result display, progress indicators)
       - File list (grid/list layout)
       - Settings panel (form layout, input styling)
       - Health indicator (dot, label, status colors)
    4. Check for:
       - Same colors, fonts, spacing
       - Same responsive breakpoints
       - Same hover states and transitions
       - Same layout structure
    5. Take screenshots of both frontends for documentation
  </how-to-verify>
  <resume-signal>Type "approved" if visual parity is confirmed, or describe any gaps found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create CSS parity report</name>
  <files>.planning/phases/05-polish-parity/css-parity-report.md</files>
  <read_first>
    - .planning/phases/05-polish-parity/css-audit-findings.md
  </read_first>
  <action>
    Create comprehensive CSS parity report:
    1. Document all components compared
    2. List any visual gaps found during verification
    3. Document fixes applied (with file references)
    4. Include screenshot comparisons (if available)
    5. Mark each component as:
       - ✓ Parity confirmed
       - ⚠ Minor differences (non-blocking)
       - ✗ Gaps requiring fixes
    6. Save to .planning/phases/05-polish-parity/css-parity-report.md
  </action>
  <verify>
    <automated>test -f .planning/phases/05-polish-parity/css-parity-report.md && grep -q "Parity confirmed" .planning/phases/05-polish-parity/css-parity-report.md</automated>
  </verify>
  <done>
    CSS parity report exists documenting visual comparison results
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS rendering | Browser interprets CSS to render UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | CSS injection via variables | accept | CSS custom properties are set by trusted codebase, no user input |
| T-05-04 | I | Layout breaks from missing styles | mitigate | Visual verification catches layout gaps before production |
</threat_model>

<verification>
1. CSS audit findings document created
2. All CSS issues fixed (variables defined, hardcoded values replaced)
3. Visual verification checkpoint passed
4. CSS parity report created with component-by-component comparison
5. Build completes successfully after CSS changes
</verification>

<success_criteria>
- All CSS custom properties used consistently
- No hardcoded values that should use variables
- Visual parity verified side-by-side
- Parity report documents all components and findings
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-parity/05-02-SUMMARY.md`
</output>
