# Production Secrets Template
# Copy this file to .secrets/production.env and fill in your actual values
# This file should never be committed to version control

# Cloudflare Tunnel Configuration
TUNNEL_TOKEN=your_cloudflare_tunnel_token_here

# Domain Configuration
DOMAIN=your_domain_here

# Traefik Dashboard Authentication
TRAEFIK_DASHBOARD_USERS=user:password_hash

# Jetson Nano Deployment Configuration
JETSON_HOST=192.168.x.x
JETSON_USER=your_user
JETSON_SUDO_PASSWORD=your_sudo_password
JETSON_APP_DIRECTORY=/opt/your_app_path
JETSON_DEPLOYMENT_GROUP=your_group

# Cloud VM Deployment Configuration
# These secrets are used by GitHub Actions workflow (docker-monorepo-build-x86.yml)
# to automatically deploy the Go server to the production cloud VM after building images.
#
# To configure these secrets in GitHub:
# 1. Go to: Settings > Secrets and variables > Actions
# 2. Add each secret using the values below
#
# CLOUD_VM_HOST: The hostname or IP address of the cloud VM
CLOUD_VM_HOST=vultur.josnelihurt.me
#
# CLOUD_VM_USER: SSH username for connecting to the cloud VM
CLOUD_VM_USER=jrb
#
# CLOUD_VM_SSH_KEY: Private SSH key for authenticating to the cloud VM
# This key must be added to the cloud VM's ~/.ssh/authorized_keys file
# Format: Full private key including BEGIN/END markers, preserve all newlines
# Example format:
# CLOUD_VM_SSH_KEY="-----BEGIN OPENSSH PRIVATE KEY-----
# <base64-encoded-key-content>
# -----END OPENSSH PRIVATE KEY-----"
#
# To generate a new SSH key pair for CI/CD:
# ssh-keygen -t ed25519 -f /tmp/ci_cloud_vm_key -N "" -C "ci-deployment-$(date +%Y%m%d)"
# Then add the public key to the VM: ssh-copy-id -i /tmp/ci_cloud_vm_key.pub user@host
# And use the private key content for CLOUD_VM_SSH_KEY
CLOUD_VM_SSH_KEY="-----BEGIN OPENSSH PRIVATE KEY-----
your_private_key_content_here
-----END OPENSSH PRIVATE KEY-----"
#
# CLOUD_VM_SUDO_PASSWORD: (Optional) Sudo password if needed for deployment tasks
# Note: Current deployment scripts do not require sudo, but this is kept for compatibility
CLOUD_VM_SUDO_PASSWORD=your_sudo_password_if_needed

