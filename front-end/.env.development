# Development environment configuration
# PLAYWRIGHT_BASE_URL: set by scripts/test/e2e.sh or your shell (defaults to vite preview in playwright.config when unset)
NODE_ENV=development
PLAYWRIGHT_TRACE=true
PLAYWRIGHT_SCREENSHOTS=true
PLAYWRIGHT_VIDEO=false
